# KUBATA KIÉ — Environment Variables Example
# Copy this file to .env and fill in the values
# NEVER commit the real .env file or real passwords

# Admin bootstrap (password is set via Supabase Auth, never hardcoded)
ADMIN_EMAIL=maldinefranciscocaxalacaxala@gmail.com
ADMIN_INITIAL_PASSWORD=CHANGE_ME

# Supabase (pre-populated automatically)
SUPABASE_URL=
SUPABASE_ANON_KEY=
SUPABASE_SERVICE_ROLE_KEY=

# Google Maps API key for location features
VITE_GOOGLE_MAPS_API_KEY=
